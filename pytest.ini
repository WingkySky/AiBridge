# AIBridge v2 测试配置（Python 绑定 e2e + 跨语言一致性探针）
#
# 说明：根目录原 pyproject.toml 是 v1（agn-sdk）的打包配置，v1 归档时已删除；
# 其中 pytest 配置段（v2 测试仍在使用）迁移到本文件。

[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v
